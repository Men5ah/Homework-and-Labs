public class TestPerson {
    public static void main(String[] args) {
 /*        Person newPerson = new Person();
        Person newerPerson = new Person(); // Test Default Constructor
        Person newestPerson = new Person("John", 20, 'M');
        Person eboPerson = new Person("Ebo", 55, 'M');
        Person ianPerson = new Person("Ian", 23, 'M');
        Person atoPerson = new Person("Ato", 50, 'M');
        Person personAto = new Person("Ato", 20, 'M');
        Person kikiPerson = new Person("Kiki", 20, 'M');
        Person grandPerson = new Person("Jane", 85, 'F');
        Person personDan = new Person("Sam", 16, 'F');
        Person personSam = new Person("Sam", 16, 'M');


        // Test Setters
        newPerson.setName("John");
        newPerson.setAge(20);
        newPerson.setGender('F');

        // Test Getters
        System.out.println("Name: " + newPerson.getName());
        System.out.println("Age: " + newPerson.getAge());
        System.out.println("Gender: " + newPerson.getGender());

        // Test default consructor.
        System.out.println("\nName: " + newerPerson.getName());
        System.out.println("Age: " + newerPerson.getAge());
        System.out.println("Gender: " + newerPerson.getGender());

        // Test second constructor
        System.out.println("\nName: " + newestPerson.getName());
        System.out.println("Age: " + newestPerson.getAge());
        System.out.println("Gender: " + newestPerson.getGender());

        newPerson.setPerson("Randy", 10, 'M');
        System.out.println(newPerson.toString());
       
        // Assignment Tests
        String isEqual;
        String sameAge;
        String isOlder;
        String isYounger;


        // Same name and age
        System.out.println("\nTest for same person (Name and Age)");
        isEqual = personDan.equals(personSam) ? "These are the same person.": "These are not the same person";
        System.out.println(isEqual);

        isEqual = personAto.equals(personSam) ? "These are the same person.": "These are not the same person";
        System.out.println(isEqual);

        //Same name
        System.out.println("\nTest for same name.");
        isEqual = atoPerson.equals(personAto) ? "These people have the same name":"These people don't have the same name";
        System.out.println(isEqual);

        isEqual = atoPerson.equals(eboPerson) ? "These people have the same name":"These people don't have the same name";
        System.out.println(isEqual);

        // Testing Ages
        // Same age.
        System.out.println("\nTesting for same age");
        sameAge = personAto.isSameAge(kikiPerson) ? "These people are the same age":"These people are not the same age";
        System.out.println(sameAge);

        sameAge = eboPerson.isSameAge(ianPerson) ? "These people are the same age":"These people are not the same age";
        System.out.println(sameAge);

        // Older
        System.out.println("\nTesting to see if one object is older than the other.");
        isOlder = eboPerson.isOlderThan(ianPerson) ? eboPerson.getName() + " is older than " + ianPerson.getName() : eboPerson.getName() + " is not older than " + ianPerson.getName();
        System.out.println(isOlder);

        isOlder = eboPerson.isOlderThan(grandPerson) ? eboPerson.getName() + " is older than " + ianPerson.getName() : eboPerson.getName() + " is not older than " + grandPerson.getName();
        System.out.println(isOlder);

        // Younger
        System.out.println("\nTesting to see if one object is younger than the other.");
        isYounger = ianPerson.isYoungerThan(eboPerson) ? ianPerson.getName() + " is younger than " + eboPerson.getName() : ianPerson.getName() + " is not younger than " + eboPerson.getName();
        System.out.println(isYounger);

        isYounger = grandPerson.isYoungerThan(eboPerson) ? grandPerson.getName() + " is younger than " + eboPerson.getName() : grandPerson.getName() + " is not younger than " + eboPerson.getName();
        System.out.println(isYounger);
 */
    }
}
